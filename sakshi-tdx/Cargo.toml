[package]
name = "sakshi-tdx"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = ["sakshi-core/std"]
[dependencies]
sakshi-core = { path = "../sakshi-core", default-features = false }
nix = { version = "0.29", features = ["ioctl"] }
