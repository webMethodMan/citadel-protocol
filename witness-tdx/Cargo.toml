[package]
name = "witness-tdx"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = ["witness-core/std"]

[dependencies]
witness-core = { path = "../witness-core", default-features = false }
nix = { version = "0.29", features = ["ioctl"] }
